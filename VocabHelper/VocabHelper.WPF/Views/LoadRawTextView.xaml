<UserControl x:Class="VocabHelper.WPF.Views.LoadRawTextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VocabHelper.WPF.Views"
             xmlns:behaviours="clr-namespace:VocabHelper.WPF.Behaviours"
             xmlns:avalonEdit="clr-namespace:ICSharpCode.AvalonEdit;assembly=ICSharpCode.AvalonEdit"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0">Language</Label>
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Languages}" SelectedItem="{Binding ChosenLanguage}" />
        <avalonEdit:TextEditor Grid.Row="1" Grid.ColumnSpan="999" FontFamily="Consolas" ShowLineNumbers="True" WordWrap="True" behaviours:AvalonEditBehaviour.BoundText="{Binding RawText, UpdateSourceTrigger=PropertyChanged}" />


        <Button Grid.Row="2" Grid.ColumnSpan="999" Command="{Binding AcceptCommand}">Accept</Button>
    </Grid>
</UserControl>
