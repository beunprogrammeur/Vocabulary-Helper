<UserControl x:Class="VocabHelper.WPF.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VocabHelper.WPF.Views"
             mc:Ignorable="d" 
            xmlns:vm="clr-namespace:VocabHelper.WPF.Business.ViewModels;assembly=VocabHelper.WPF.Business"
             xmlns:v="clr-namespace:VocabHelper.WPF.Views"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <TabControl Grid.Row="0">
            <TabItem Header="Dictionary">
                <v:EBookView DataContext="{Binding DictionaryViewModel}"/>
            </TabItem>
            <TabItem Header="Text Loading">
                <v:LoadRawTextView DataContext="{Binding TextLoadViewModel}" />
            </TabItem>
            <TabItem Header="Matching">
                <v:CandidateToCardMappingView DataContext="{Binding CandidateToCardMappingViewModel}" />
            </TabItem>
            <TabItem Header="Logs">
                TBD
            </TabItem>
            <TabItem Header="Settings">
                TBD
            </TabItem>
        </TabControl>
        <v:StatusView Grid.Row="1" Grid.ColumnSpan="999" DataContext="{Binding StatusViewModel}" />
    </Grid>
    
    
    
</UserControl>
