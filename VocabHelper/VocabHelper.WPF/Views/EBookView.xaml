<UserControl x:Class="VocabHelper.WPF.Views.EBookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VocabHelper.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30px" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Menu Grid.Row="0" Grid.ColumnSpan="999">
            <MenuItem Header="_Load">
                <MenuItem Header="_Load Raw Text" Command="{Binding LoadRawTextCommand}" />
                <MenuItem Header="_Clear" Command="{Binding ClearDataCommand}" />
            </MenuItem>
            <MenuItem Header="_Translate">
                <MenuItem Header="_Google Translate" Command="{Binding TranslateCardsCommand}" />
            </MenuItem>
            <MenuItem Header="_Anki">
                <MenuItem Header="Load Decks" Command="{Binding LoadAnkiDecksAsyncCommand}"/>
                <MenuItem Header="Ignore cards from Match decks" Command="{Binding MarkCandidatesIgnoredFromDeckCommand}"/>
                <MenuItem Header="Start mapping process" Command="{Binding MappingCommand}" />
                <MenuItem Header="Import into anki" Command="{Binding ImportCommand}" />
            </MenuItem>
        </Menu>
        <GroupBox Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Header="Book vocabulary">
            <DataGrid ItemsSource="{Binding CardCandidates}" AutoGenerateColumns="False" CanUserAddRows="false">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Ignore" Binding="{Binding IsIgnored, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Index" Binding="{Binding Index}" IsReadOnly="True" Width="Auto"/>
                    <DataGridTextColumn Header="Frequency" Binding="{Binding Frequency}" IsReadOnly="True" Width="Auto"/>
                    <DataGridTextColumn Header="Word" Binding="{Binding Word, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Translation" Binding="{Binding WordTranslation}" />
                    <DataGridTextColumn Header="Sentence" Binding="{Binding Sentence}" Width="*"/>
                    <DataGridTextColumn Header="Translation" Binding="{Binding SentenceTranslation}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        <GroupBox Header="Anki Decks"  Grid.Row="1" >
            <DataGrid ItemsSource="{Binding AnkiDecks}" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Match" Binding="{Binding Match, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Name" Binding="{Binding DeckName}" Width="*" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

    </Grid>
</UserControl>
