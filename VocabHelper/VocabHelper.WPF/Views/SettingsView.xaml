<UserControl x:Class="VocabHelper.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VocabHelper.WPF.Views"
             xmlns:avalonEdit="clr-namespace:ICSharpCode.AvalonEdit;assembly=ICSharpCode.AvalonEdit"
             xmlns:i="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
             xmlns:behaviours="clr-namespace:VocabHelper.WPF.Behaviours"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GroupBox Header="Language Settings" Grid.Row="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition Height="30" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" >Source Language</Label>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding AppSettings.LanguageSettings.SourceLanguage}"/>
                <Label Grid.Row="1" Grid.Column="0">Target Language</Label>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding AppSettings.LanguageSettings.TargetLanguage}" />
            </Grid>
        </GroupBox>
        <GroupBox Header="API Settings" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <Label Grid.Row="0" Grid.Column="0">API endpoint</Label>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding AppSettings.ApiSettings.ApiEndpoint}"/>

                <GroupBox Header="System prompt" Grid.Row="1" Grid.ColumnSpan="999" >
                    <avalonEdit:TextEditor FontFamily="Consolas" ShowLineNumbers="True" WordWrap="True" SyntaxHighlighting="MarkDown" behaviours:AvalonEditBehaviour.BoundText="{Binding AppSettings.ApiSettings.SystemPrompt, UpdateSourceTrigger=PropertyChanged}" >
                    </avalonEdit:TextEditor>
                </GroupBox>
                

            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
