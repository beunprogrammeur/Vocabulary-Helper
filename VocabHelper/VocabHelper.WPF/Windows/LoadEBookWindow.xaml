<Window x:Class="VocabHelper.WPF.Windows.LoadEBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VocabHelper.WPF.Windows"
        mc:Ignorable="d"
        Title="DragAndDropWindow" Height="450" Width="450"
        AllowDrop="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GroupBox Header="Settings" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <!-- row 0 -->
                <Label Grid.Column="0" >Language</Label>
                <ComboBox Grid.Column="1" ItemsSource="{Binding Languages}" SelectedItem="{Binding ChosenLanguage}"/>

                <!-- row 1 -->
                <Label Grid.Row="1" Grid.ColumnSpan="999" Content="{Binding FilePath, StringFormat='File path: {0}'}"></Label>

                <!-- row 2 -->
                <Button Grid.Row="2" Grid.ColumnSpan="999" Command="{Binding ContinueCommand}">Continue</Button>
            </Grid>
                
        </GroupBox>
        <GroupBox Header="EBook drop area" Grid.Row="1">
            <Border AllowDrop="True"
                Drop="Window_Drop"
                DragOver="Window_DragOver"
        
                Background="#22FFFFFF"
                Padding="40"
                CornerRadius="8">
                <TextBlock Text="Drop EPUB file here" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </GroupBox>
    </Grid>
    
    

</Window>
